services:
  - type: web
    name: hemani-portfolio
    env: node
    plan: free
    buildCommand: cd client && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production 